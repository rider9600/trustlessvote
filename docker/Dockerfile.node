FROM node:22-alpine

WORKDIR /app

# Install deps first for better Docker layer caching
COPY package.json package-lock.json* ./
RUN npm ci || npm install

# Copy the rest (for non-volume use cases)
COPY . .

EXPOSE 5173
