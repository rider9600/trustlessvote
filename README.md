## Running the Project with Docker (Recommended)

This project uses Docker to simplify setup and ensure reproducibility.
No local installation of Node.js, Hardhat, or Solidity is required.

### Prerequisites
- Docker
- Docker Compose

### Run
docker-compose build
docker-compose up

Backend compiles smart contracts using Hardhat.
Frontend runs a React app at http://localhost:3000
